rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /documents/{allPaths=**} {
      // Allow authenticated users to upload and read document files
      allow read, write: if request.auth != null;
    }
  }
}
